{% extends 'base.html.twig' %}

{% block content %}

    <div id="container_main">

        <div id="container_infos">

            <h1>Vous souhaitez nous contacter ?</h1>
            <h3>Nous serions ravis de discuter avec vous.</h3>
            <h4 class="needToMove">Adresse :</h4><p>20 Rue du Truc 75000 Paris</p>
            <h4>Numéro de téléphone :</h4><p>05 76 54 27 83</p>
            <h4>Adresse e-mail :</h4><p>infostourismecontact@gmail.com</p>
            <p>Ou remplissez directement notre formulaire de contact,<br>
                nous vous apporterons une réponse rapide.</p>

        </div>

        <div id="container_form">

            {{ form_start(contactF, {'attr': {'id': 'contact'}} ) }}

                <div class="my-custom-class-for-errors">
                    {{ form_errors(contactF) }}
                </div>

                {% for message in app.flashes('message') %}
                    <div class="alert alert-success" role="alert">
                        {{ message }}
                    </div>
                {% endfor %}

                <h3>Formulaire de contact</h3>
                <h4>Un problème ? Une question ? N'hésitez pas à nous contacter !</h4>
                    
                    <div >
                        {{ form_widget(contactF.name, {'attr': {'placeholder': 'Nom & prénom'}}) }}
                    </div>

                    <div >
                        {{ form_widget(contactF.email, {'attr': {'placeholder': 'Adresse e-mail'}}) }}
                    </div>

                    <div >
                        {{ form_widget(contactF.subject, {'attr': {'placeholder': 'Objet'}}) }}
                    </div>

                    <div >
                        {{ form_widget(contactF.message, {'attr': {'placeholder': 'Entrez votre message ici...'}}) }}
                    </div>

            {{ form_end(contactF) }}

        </div>

    </div>
        

{% endblock %}